Design

The method fuzzy_clock returns the fuzzy time in string format when parameters hour and minute is passed
The program prints fuzzy times until it is stopped

Dependencies
Standard Python library

Usage 

Run the python code
